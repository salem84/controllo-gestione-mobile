using System;
using ControlloGestione.Services;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace ControlloGestione.Tests.Services
{
    [TestClass]
    public class FirmeServiceTest
    {
        [TestMethod]
        public void Autentica()
        {
            /*FirmeService service = new FirmeService();

            string username = "";
            string password = "";
            service.Autentica(username, password);
            string page = service.ReadCtrlGestionePage();

            OrariParserService parser = new OrariParserService();
            parser.ReadHours(page);*/

            Assert.IsTrue(true);
        }
    }
}
